{
  "name": "tdd-mocha-chai",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test:prd": "set ENVIRONMENT=prd & mocha tests/**/*.js --reporter nyan --exit",
    "test:local": "set ENVIRONMENT=local & mocha tests/**/*.js --reporter nyan --exit",
    "landing_test": "mocha tests/*.js --reporter landing --exit"
  },
  "author": "Jacques Schmitz Junior",
  "license": "ISC",
  "devDependencies": {
    "ajv": "^8.6.2",
    "chai": "^4.3.4",
    "chai-http": "^4.3.0",
    "faker": "^5.5.3",
    "mocha": "^9.1.0",
    "mocha-junit-reporter": "^2.0.0",
    "mochawesome": "^6.2.2"
  }
}
